services:
  api:
    build: .
    container_name: api-registro-matricula
    restart: unless-stopped
    ports:
      - "5309:5309"                 
    environment:
      - NODE_ENV=production        
    volumes:
      - ./logs:/app/logs
    